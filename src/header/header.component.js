import templateHeader from "./header.html"
import HeaderController from "./header.controller"

const headerComponent = {
    template: templateHeader,
    controller: HeaderController
}

export default headerComponent